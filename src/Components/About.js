import React, { useState, useEffect, useContext } from "react";

const About = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <p>We are amazeballs</p>
    </div>
  );
};
export default About;
